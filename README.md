# Arcade-Cabinet

This repository contains the source code for the GCS Arcade Cabinet game launcher and executables for games accessabile in the launcher.

The launcher can be started via /launcher/build/CabinetGameSelect.exe
The launcher only works on Windows since it directly runs executables from spawned processes.